# Poker Dealer Phased Delivery Plan

## Outcome And Owned Paths

Turn the existing Stage 00–5 documents into one dependency-aware delivery
roadmap centred on four verifiable loops: player-action evidence and focus
switching, phase-aware table/card perception, authoritative game/ledger state,
and safe physical dealing. Each stage must state entry conditions, bounded
work packages, artifacts, validation evidence, exit Gate, rollback and parallel
work.

Owned paths: `docs/plans/POKER_DEALER_MASTER_PLAN.md`, `docs/stages/`,
`docs/evaluation/stage-0-gate-audit.md`, and this target plan.

## Dirty Paths Left Read-Only

All source, tests, configs/schemas, archived DeskMate material, camera work,
model manifest, robotics implementation, datasets and prior target plans remain
read-only. This target changes planning only and does not claim implementation.

## External Dependencies

- Product confirmation of S0-07 betting structure and the S0-16/18 gesture and
  confirmation interaction.
- Target-camera/table evidence for four action regions, thirteen card slots,
  deck/session coverage and phase-aware scene states.
- Robotics evidence for feeder, reveal, target geometry, sensors, safety and
  protocol framing.
- Four-player participants and an operator for later controlled evaluation.

## Validation And Physical Motion

Check that every stage has explicit entry/exit Gates, artifacts, owners,
dependencies, failure fallback and no circular authority; validate all local
Markdown links, stale stage names/counts and `git diff --check`. No camera,
training, hardware connection or physical motion is involved.

Completed on 2026-07-21: Stage 00–5 each contains an entry/current-state
boundary, explicit Gate and failure/rollback rule; the master roadmap contains
the dependency graph, stage readiness, milestones and five suggested
iterations; all 18 active Markdown local links resolved; no stale pre-migration
stage names/counts remained; and `git diff --check` passed. This was planning
validation only—no implementation, model training, camera use or robot motion
was performed.

## Commit Intent

Do not commit or push unless the user explicitly requests it.
