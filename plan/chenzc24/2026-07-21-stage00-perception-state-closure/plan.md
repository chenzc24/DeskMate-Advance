# Stage 00 Perception And State Closure

## Outcome And Owned Paths

Close the Stage 00 gap between player-behaviour Deep Learning evidence and the
deterministic game state machine. Freeze state-controlled seat attention,
temporal action evidence, phase-aware table-slot state, authoritative digital
ledger updates and append-only audit semantics without choosing unmeasured
gesture geometry or confidence thresholds.

Owned paths: active Poker Dealer governance and packaging, `configs/game/`,
`configs/contracts/`, `src/poker_dealer/domain/`, `tests/contracts/`,
`tests/domain/`, `docs/plans/`, `docs/stages/`, `docs/architecture/`,
`docs/contracts/`, `docs/evaluation/`, and this target plan.

## Dirty Paths Left Read-Only

DeskMate removals and archive, model manifest, camera source/tests/scripts,
robotics implementation, local datasets/assets and prior target plans remain
read-only. Do not change physical geometry, select a model release or modify
the archive branch.

## External Dependencies

- Target-camera recordings for all four fixed player action zones, ordinary
  non-game motion, occlusion, simultaneous movement and target gestures.
- Human selection of the exact gesture grammar and whether high-risk actions
  require an explicit feedback/confirmation interaction.
- Target-table evidence for card occupancy, face orientation, reveal and
  thirteen-slot lifecycle thresholds.
- Product confirmation of the final betting structure remains under S0-07.

## Validation And Physical Motion

Validate all JSON schemas/examples, exact Python/JSON vocabulary, decision IDs
and status counts, contract version references, active Markdown links, stale
direct-model-authority wording, targeted and full tests, and
`git diff --check`. No camera capture, hardware connection or physical robot
motion is involved.

Completed on 2026-07-21: the full practical suite passed 37 tests; all 19 JSON
files parsed and every Draft 2020-12 schema/example validated; local schema
references and all 17 active Markdown links resolved; S0-01 through S0-20 were
complete with status counts 4 frozen / 11 partially frozen / 5 evidence
required; 18 walkthroughs, 10 mechanical targets, 13 card slots and 4 action
regions were present; stale pre-supplement rule references were absent; and
`git diff --check` passed. Validation used static contracts and simulation only;
no camera or robot was operated.

## Commit Intent

Do not commit or push unless the user explicitly requests it.
